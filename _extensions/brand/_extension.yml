title: Brand
author: Tyler Pollard
version: 1.0.2
quarto-required: ">=1.8.0"
contributes:
  metadata:
    # project:
      # Default to light brand for compatibility in extension metadata.
      # Use top-level `brand.light` / `brand.dark` in consumer _quarto.yml
      # to activate both standalone files.
    brand:
      light: _extensions/brand/light-brand.yml
      dark: _extensions/brand/dark-brand.yml
